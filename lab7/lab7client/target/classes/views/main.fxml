<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
            fx:controller="ru.tmchhhhhhhhhhhhh.javalabs.lab7client.client.gui.controllers.MainController"
            prefHeight="700.0" prefWidth="1100.0">

    <top>
        <VBox styleClass="header">
            <Label text="🍽️ СИСТЕМА УПРАВЛЕНИЯ РЕСТОРАНОМ - HIBERNATE"
                   style="-fx-font-size: 24px; -fx-text-fill: white; -fx-font-weight: bold; -fx-padding: 15px;"/>
        </VBox>
    </top>

    <center>
        <VBox spacing="10" VBox.vgrow="ALWAYS">
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>

            <HBox spacing="10" alignment="CENTER_LEFT">
                <Label text="МЕНЮ РЕСТОРАНА" style="-fx-font-size: 18px; -fx-font-weight: bold;"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button text="🔄 Обновить" onAction="#handleRefresh" styleClass="btn-primary"/>
            </HBox>

            <TableView fx:id="dishTable" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="typeColumn" text="Тип" prefWidth="120"/>
                    <TableColumn fx:id="nameColumn" text="Название" prefWidth="200"/>
                    <TableColumn fx:id="priceColumn" text="Цена" prefWidth="120"/>
                    <TableColumn fx:id="caloriesColumn" text="Калории" prefWidth="120"/>
                    <TableColumn fx:id="ingredientsColumn" text="Ингредиенты" prefWidth="420"/>
                </columns>
            </TableView>
        </VBox>
    </center>

    <right>
        <VBox spacing="10" styleClass="sidebar">
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>

            <Label text="УПРАВЛЕНИЕ" style="-fx-font-weight: bold; -fx-font-size: 14px;"/>
            <Separator/>

            <Button text="➕ Добавить блюдо" onAction="#handleAdd" maxWidth="Infinity" styleClass="btn-success"/>
            <Button text="🗑️ Удалить" onAction="#handleDelete" maxWidth="Infinity" styleClass="btn-danger"/>

            <Separator/>
            <Label text="ФИЛЬТРЫ" style="-fx-font-weight: bold;"/>
            <Button text="🔍 По типу" onAction="#handleFilterByType" maxWidth="Infinity"/>

            <Separator/>
            <Label text="СОРТИРОВКА" style="-fx-font-weight: bold;"/>
            <Button text="💵 По цене" onAction="#handleSortByPrice" maxWidth="Infinity"/>

            <Separator/>
            <Button text="📊 Статистика" onAction="#handleStatistics" maxWidth="Infinity" styleClass="btn-info"/>
            <Button text="📝 Заказать" onAction="#handleOrder" maxWidth="Infinity" styleClass="btn-success"/>
        </VBox>
    </right>

    <bottom>
        <HBox styleClass="status-bar" alignment="CENTER_LEFT" spacing="20">
            <padding>
                <Insets top="5" right="10" bottom="5" left="10"/>
            </padding>
            <Label fx:id="statusLabel" text="Готово" style="-fx-text-fill: white;"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label text="🟢 Hibernate + PostgreSQL" style="-fx-text-fill: white;"/>
        </HBox>
    </bottom>

</BorderPane>